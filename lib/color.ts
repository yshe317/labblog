import { createTheme } from '@mui/material/styles';

export const headertheme = createTheme({
    palette: {
        primary: {
          main: '#000000',
        },
        secondary: {
          main: '#000000',
        },
      },
});
